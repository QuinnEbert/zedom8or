<?php
/*
 * Z8Engine.php >> "The Z8 Engine"
 * Part of Quinn Ebert's Zedom8or Project
 * 
 * PURPOSE:
 * Z8 Engine handles the task of managing storage, programming, and execution of
 * multi-device (and single-device) automation programmes for Zedom8or.
 */

// Attempt to load classes for devices that need them:
/*foreach ($devices as $device) {
	if ($device['control'] == 'phpClass') {
		$modFile = './DevicesLibrary/'.$device['class'].'.php';
		if (!file_exists($modFile)) {
			die('Cannot load module: '.$modFile);
		} else {
			require_once($modFile);
		}
	}
}*/

?>